TODO list (highly volatile / not really a public doc):
- [ ] aliases need to go away
- [ ] publicly-exposed thrift needs to go away
- [ ] publicly-exposed proto needs to go away
  - this can probably just become "it's in the proto submodule, with thrift2proto impl"
- [ ] need a linter to detect 3rd party library exposure in our APIs
  - this can probably just be an Analyzer that iterates all public types, recursively, until it finds a different module.
    - and a list of allowed vs banned, and it needs to fail if an undeclared one is exposed.  else new exposures of new libraries sneak past. 
  - in particular this is needed to mechanically ban thrift exposure
  - we should also consider doing this for tally, as the tally/v4 change showed
  - ... opentelemetry?  metrics?  others?
  - I suspect it'll show a fair bit more than we expect
- find other "needs to be modernized" apis, like NewDisconnectedContext -> WithoutCancel
- [ ] need to wrap all errors to break any `err == othererr` or `err.(type)` checks.
  - and remove any constant error values. _require_ `errors.As` from day 1.
  - then we can start adding data to them.
- [ ] ... can we realistically get rid of global registration in v2?  likely need some more helper tools...